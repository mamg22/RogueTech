{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Flask server @ 8080",
            "type": "shell",
            "command": "flask --debug run -h 0 --port 8080",
            "isBackground": true,
            "runOptions": {
                "runOn": "folderOpen"
            },
            "problemMatcher": [],
            "options": {
                "env": {
                    "FLASK_ENV": "development"
                }
            }
        }
    ]
}